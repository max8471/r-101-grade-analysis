---
title: "GradeAnalysis"
author: "Max"
format: 
  html:
    message: false
    echo: false
---

```{r}
here::i_am("r-101-grade-analysis.Rproj")
library(here)
library(ggplot2)
library(tidyr)
library(dplyr)
library(vroom)
```

```{r}
grades<-vroom(here("grades.csv"))
```
## Question 2

```{r}
grades|>
  summarise(MinGrade=min(`Exam`,na.rm=TRUE),MaxGrade=max(`Exam`,na.rm=TRUE),MedianGrade=median(`Exam`,na.rm=TRUE),AvgGrade=mean(`Exam`,na.rm=TRUE))|>
  knitr::kable()
```


## Question 3
```{r}
nb_stud_exam<-sum(is.na(grades$Exam))
```
There is a total of `r nb_stud_exam` students who passed the test

## Question 4
```{r}
ggplot(grades, aes(x=Exam))+
  geom_histogram(binwidth=2.5,fill="darkblue",color="black",alpha=0.7,na.rm=TRUE)+
  labs(title= "Distribution of Exam grades",
       x="Exam grades",
       y= "Frequency")
```
## Question 5
```{r}
grades|>
  group_by(Group)|>
  summarise(N=n())
```
## Question 7 
a)
```{r}
ggplot(grades,aes(x=Group, y=Exam, fill=Group))+
  geom_boxplot()+
  labs(title="Distribution of exams grades by group", x="Group",y="Exam grades")
```
b)
```{r}
ggplot(grades, aes(x=Exam, fill=Group))+
  geom_density()+
  labs(title="Distribution of exams grades by group", x="Exam grades",y="Density")+facet_wrap(~Group, ncol=4)
```


